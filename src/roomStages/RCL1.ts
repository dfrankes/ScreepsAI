import RoomManager from "managers/RoomManager";

export default function roomStage(roomManager : RoomManager){
	console.log(`running room "${roomManager.room.name}" till RCL2`);





	// Return creep work
}
